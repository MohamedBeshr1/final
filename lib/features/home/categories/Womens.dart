import 'package:flutter/material.dart';

class WomensPage extends StatelessWidget {
  const WomensPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Womens")),
      body: const Center(child: Text("Womens Page")),
    );
  }
}
